# 3.5-close #1.7 — banned overclaim vocabulary (scoped, NARROW phrases only).
# One phrase per line, case-insensitive substring match against Python STRING LITERALS (docstrings +
# constants; comments are exempt) and the designated Markdown docs. Deliberately NOT common single
# words ("verified" / "only" / "pinned") — those are legitimate almost everywhere; banning them would
# force a huge suppressions file (the theatre this guard avoids). Only unambiguous absolute-safety /
# execution-assurance / guarantee / un-bypassable / fully-bound claims belong here.
every confound closed
all confounds closed
execution-assured
execution assurance
runtime-behaviour assured
proven safe
proven secure
cryptographically unassailable
un-bypassable
unbypassable
cannot be bypassed
absolutely safe
guarantees execution
guaranteed safe
fully-bound
genuinely blind
# CP2 S7 — narrow PROOF / PLAN-CONTEXT overclaim phrases. The run-admission proof is a trusted in-process
# call-path convention, and AuthorizedRunPlan is API cohesion — NEITHER is an unforgeable boundary. These
# ASSERTIVE phrases claim otherwise and are collision-free (they appear nowhere legitimately). Deliberately
# NOT the bare words "unforgeable" / "impossible" / "never" / "guarantee" / "cryptographic": those have
# legitimate NEGATED disclaimers ("NOT an unforgeable boundary"), real asymmetric-crypto uses ("holds only
# the public key, cannot forge"), enforced-invariant / by-construction uses, and honest fundamental-limit
# claims — banning the bare word would force a growing suppressions file, the theatre this guard avoids.
unconstructible
provably unforgeable
unforgeable capability
unforgeable-by-convention
sealed token
sealed-token
